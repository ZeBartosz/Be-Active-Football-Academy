import ProgramGroupSection from "../Components/Home/ProgramGroupSection.jsx";

export default function ProgramGroupIndex({ programGroups }) {
    return (
        <>
            <ProgramGroupSection programs={programGroups} />
        </>
    );
}
